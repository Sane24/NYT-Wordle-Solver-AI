# Copy this file to bot/.env and fill in your values from
# https://discord.com/developers/applications  (New Application → Bot)
DISCORD_TOKEN=your-bot-token
CLIENT_ID=your-application-id
# Optional: register commands instantly in one server while developing.
# Leave empty to register globally (takes up to an hour to appear).
GUILD_ID=
